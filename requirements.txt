# === Core RAG dependencies ===
openai>=1.30.0          # Cloud LLM + embeddings backend
chromadb>=0.5.0         # Local vector store (no API key needed, supports offline story)
langchain>=0.2.0        # Document loading and text splitting utilities
langchain-community>=0.2.0

# === Document ingestion ===
pypdf>=4.0.0            # PDF parsing
python-docx>=1.0.0      # Word doc parsing

# === Environment + config ===
python-dotenv>=1.0.0    # Load .env file for API keys

# === CLI ===
rich>=13.0.0            # Pretty terminal output (makes demos look good)

# === Evaluation (Phase 1 basic) ===
pandas>=2.0.0           # Test set management
